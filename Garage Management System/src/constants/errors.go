package constant

//token related errors
const (
	WrongSigningAlgorithmError = "wrong signing algorithm error"
	TokenExpiredError          = "token is expired"
	ClaimMappingFailedError    = "error in the claims mapping"
)
